@EndUserText.label: 'Flight'
@MappingRole: true
define role Z00_FLIGHT {
 grant select on Z00_FLIGHT
   where (CarrierID) = aspect pfcg_auth(S_CARRID, CARRID, ACTVT = '03');
                        
}